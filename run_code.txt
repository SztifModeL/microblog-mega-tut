# uruchamianie aplikacji
set FLASK_APP=microblog.py  # wskazywanie w wirtualnym srodowisku gdzie mamy aplikacje we flasku
flask run

# debugdumped
set FLASK_ENV=development  # eksportownie zmiennej srodowiskowaj FLASK_ENV i ustawic ja na tryb development przed uruchomieniem serwera
set FLASK_DEBUG=1  # kontrola trybu debugowania niezaleznie od srodowiska

# inicjalizowanie powloki z załadowanym kontekstem aplikacji (tak jakby byla odpalona aplikacja - mozna wykonywac wszelkie operacje)
flask shell

###########################################
# testowanie developersko aplikacji z shellem
set FLASK_APP=microblog.py
flask shell

###########################################
# uruchamianie developersko aplikacji
set FLASK_APP=microblog.py
set FLASK_DEBUG=1
set WERKZEUG_DEBUG_PIN=off
flask run